{
  "delivery": {
    "mode": "local",
    "baseBranch": "main",
    "branchPrefix": "gen-prd",
    "ci": {
      "watch": true,
      "maxFixAttempts": 2,
      "timeoutSeconds": 240
    }
  },
  "worktree": {
    "enabled": true,
    "link": [],
    "copy": [],
    "setup": []
  },
  "principles": [
    "~/projects/oh-my-principle"
  ],
  "judge": {
    "retryBudget": 15
  },
  "verify": {
    "commands": {
      "test": "CARGO_TARGET_DIR=/tmp/herdr-ide-verify/cargo cargo test --manifest-path herdr-core/Cargo.toml && cd macos && swift test --scratch-path /tmp/herdr-ide-verify/swift",
      "build": "CARGO_TARGET_DIR=/tmp/herdr-ide-verify/cargo cargo build --manifest-path herdr-core/Cargo.toml && cd macos && swift build --scratch-path /tmp/herdr-ide-verify/swift"
    }
  }
}
