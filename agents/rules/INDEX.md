# Rules Ledger

One row per learned rule. Bodies live at the landing path; this file is
metadata plus the evidence trail. Maintained by `rules add`; do not edit by hand.

| ID | Kind | Status | Landing | Evidence | Summary |
| --- | --- | --- | --- | --- | --- |
| FACT-dev-runtime-instances | fact | active | `docs/dev-runtime.md` | session 5e65ea58 (2026-08-17): pet-not-visible x2 and stray-dashboard x1, all caused by duplicate/stale instances | Verify pet changes against exactly one instance, and against the installed bundle, not cargo tauri dev |
| FACT-drag-native-loop | fact | active | `docs/pet-window-macos.md` | session 2a22f53f (2026-08-18): rAF/IPC drag stuttered (WKWebView throttling), then get_pet_state disk IO on main thread caused periodic hitches | Pet drag is a native 8ms Rust loop with zero webview IPC mid-gesture; sync Tauri commands run on the main thread, so heavy periodic commands must be async or drag-gated |
| FACT-pet-window-macos | fact | active | `docs/pet-window-macos.md` | session 5e65ea58 (2026-08-17) grey backdrop + vanished pet + Ghostty focus failure; codex 01a00e1f drag diagnosis | macOS pet window constraints: shadow off in BOTH tauri.conf.json, off-screen position guards, drag must start in mousedown, raise terminal by pid not window title |
| FACT-status-model | fact | active | `docs/status-model.md` | session 5e65ea58 (2026-08-17): dashboard disagreed with herdr sidebar | Pet pose priority (working first) and the 4-color badge row; acknowledged herdr states drop out of the list |
| INV-herdr-unseen-token | invariant | active | `agents/rules/invariants/INV-herdr-unseen-token.md` | session 5e65ea58 (2026-08-17): acknowledged `?` agents were promoted to yellow waiting because the mapper matched on the `status_question` prefix, so the dashboard disagreed with the herdr sidebar | herdr distinguishes unseen attention state (`status_question_new`, `status_approval_new`, `status_error_new`, or the legacy boolean form) from state the user has already acknowledged (`status_question` with a string value, whose `agent_status` herdr drops back to `idle`). Only unseen tokens may be promoted to attention or error; acknowledged ones must fall through to idle and disappear from the dashboard list, matching what the herdr sidebar shows. Any change to the herdr token mapping or the status aggregation must keep the `crates/herdr-core/src/herdr.rs` status tests green. |
| INV-pet-state-off-main-thread | invariant | active | `agents/rules/invariants/INV-pet-state-off-main-thread.md` | session 2a22f53f (2026-08-18): sync get_pet_state read agent files on the main thread every 1000ms and caused periodic hitches during fast drags | `get_pet_state` reads agent files off disk. A sync Tauri command runs on the main thread, where the 8ms native drag loop also runs, so this command must stay `async` (thread-pool execution). Reverting it to a sync fn reintroduces periodic drag hitches. |
| REG-window-position-offscreen | regression | active | `crates/herdr-core/src/window.rs` | session 5e65ea58 (2026-08-17): ~/.config/herdr-pet/window.json held [542720,163840]; show() succeeded with nothing visible | Off-screen saved window positions are rejected on restore and clamped on move |
