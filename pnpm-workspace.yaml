packages:
  - web
  - desktop

# Build scripts run only for these packages; pnpm 10 reads this at the
# workspace root, not from a member package.json. `electron` needs none: it
# downloads its runtime on first launch.
onlyBuiltDependencies:
  - esbuild
