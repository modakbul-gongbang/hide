--- before/section-7-acceptance-criteria.txt
+++ after/section-7-acceptance-criteria.txt
@@ -1,15 +1,17 @@
-- AC1. 한글 IME 후보창이 터미널과 에디터 양쪽에서 커서 위치를 따라간다.
-- AC2. 조합 중 백스페이스가 PTY로 전달되지 않고 조합을 지운다.
-- AC3. 두 글자 이상 조합 문자열이 인접한 다음 글자를 덮지 않는다.
-- AC4. 사이드바 항목을 선택하면 herdr가 보고하는 focused pane이 그 pane으로 바뀐다.
-- AC5. chromux가 PATH에 없을 때 브라우저 열기가 비활성 상태이고 그 사유가 화면에 보인다.
-- AC6. chromux Chrome이 이미 실행 중일 때 두 번째 열기가 새 인스턴스를 만들지 않는다.
-- AC7. 원격 SSH 연결이 끊기면 빈 목록이 아니라 끊김 사실과 재연결 결과가 화면에 보인다.
-- AC8. workspace 7 / pane 11 구성에서 herdr-ide 프로세스 RSS가 400MB 이하다.
-- AC9. `9.3`의 UI 품질 6항목이 사람 리뷰를 통과한다.
-- AC10. 알 수 없는 이벤트 `kind`를 코어에 넣으면 무시되지 않고 다음 스냅샷의 `status.last_error`에 나타난다.
-- AC11. `SSH_AUTH_SOCK`이 없는 상태에서 앱은 정상적으로 뜨고, 원격 기능만 비활성 상태로 그 사유가 화면에 보이며, 표시된 메시지에 환경변수 값이 포함되지 않는다.
-- AC12. Xcode가 설치되지 않은 상태에서 빌드 산출물이 macOS 14.0+ arm64 `.app`으로 실행된다.
-- AC13. 조립 스크립트를 연속 두 번 실행한 결과가 첫 실행 결과와 같다.
-- AC14. working 상태 pane을 닫으려 하면 확인 다이얼로그가 뜨고 그 프로세스에 무슨 일이 생기는지 문장으로 표시된다.
-- AC15. 워크벤치 저장이 실패하면 편집 내용이 유지되고 실패 사유가 표시된다.
+| ID | Criterion | Judgment | Evidence Declaration |
+| --- | --- | --- | --- |
+| AC1 | 한글 IME 후보창이 터미널과 에디터 양쪽에서 커서 위치를 따라간다. | machine+gate:human | 물리 키보드 V9 실행의 터미널·에디터 후보창 위치 증거와 사용자 판정 |
+| AC2 | 조합 중 백스페이스가 PTY로 전달되지 않고 조합을 지운다. | machine+gate:human | 물리 키보드 V9 실행의 조합 상태·Rust 바이트 trace와 사용자 판정 |
+| AC3 | 두 글자 이상 조합 문자열이 인접한 다음 글자를 덮지 않는다. | machine+gate:human | 물리 키보드 V9 실행의 터미널·에디터 렌더 증거와 사용자 판정 |
+| AC4 | 사이드바 항목을 선택하면 herdr가 보고하는 focused pane이 그 pane으로 바뀐다. | judged | 소유 fixture 선택 전후의 UI snapshot과 herdr focused pane 상태 |
+| AC5 | chromux가 PATH에 없을 때 브라우저 열기가 비활성 상태이고 그 사유가 화면에 보인다. | judged | 앱 프로세스에만 PATH를 가린 실행의 비활성 버튼·사유 화면과 상태 snapshot |
+| AC6 | chromux Chrome이 이미 실행 중일 때 두 번째 열기가 새 인스턴스를 만들지 않는다. | judged | default 프로필 재사용 전후의 `chromux ps --json`, 앱 상태, UI snapshot |
+| AC7 | 원격 SSH 연결이 끊기면 빈 목록이 아니라 끊김 사실과 재연결 결과가 화면에 보인다. | judged | 승인된 원격 연결 전환 실행의 끊김·재연결 UI snapshot과 structured status |
+| AC8 | workspace 7 / pane 11 구성에서 herdr-ide 프로세스 RSS가 400MB 이하다. | machine | - |
+| AC9 | `9.3`의 UI 품질 6항목이 사람 리뷰를 통과한다. | judged | 3a와 v1 시점의 실제 앱 screenshot, 창 metadata, 6항목 사람 리뷰 판정 |
+| AC10 | 알 수 없는 이벤트 `kind`를 코어에 넣으면 무시되지 않고 다음 스냅샷의 `status.last_error`에 나타난다. | machine | - |
+| AC11 | `SSH_AUTH_SOCK`이 없는 상태에서 앱은 정상적으로 뜨고, 원격 기능만 비활성 상태로 그 사유가 화면에 보이며, 표시된 메시지에 환경변수 값이 포함되지 않는다. | judged | 앱 프로세스 환경만 제한한 실행의 UI snapshot, structured status, 비밀값 비노출 검사 |
+| AC12 | Xcode가 설치되지 않은 상태에서 빌드 산출물이 macOS 14.0+ arm64 `.app`으로 실행된다. | machine | - |
+| AC13 | 조립 스크립트를 연속 두 번 실행한 결과가 첫 실행 결과와 같다. | machine | - |
+| AC14 | working 상태 pane을 닫으려 하면 확인 다이얼로그가 뜨고 그 프로세스에 무슨 일이 생기는지 문장으로 표시된다. | judged | 접두어 소유 fixture의 확인 다이얼로그 screenshot과 취소·확인 결과 trace |
+| AC15 | 워크벤치 저장이 실패하면 편집 내용이 유지되고 실패 사유가 표시된다. | judged | 소유 로컬 fixture의 저장 실패 전후 화면, 유지된 편집 버퍼, 디스크 불변 증거 |
