HERDR IDE NATIVE TERMINAL

This is a deterministic local Vim fixture.
Hide displays the Herdr terminal stream through its native terminal view.
The fixture exercises text, cursor, selection, scrolling, and alternate-screen behavior.

한글 입력과 Option Meta 입력은 같은 실제 PTY로 전달됩니다.
No diagnostic transcript or raw escape sequence belongs in product mode.

T5 evidence state
- real Herdr pane attach
- real alternate-screen TUI
- cursor and styled terminal cells
- Korean UTF-8 content
- product UI without TERMINAL A or IME diagnostic labels
